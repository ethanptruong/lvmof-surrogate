# Core scientific stack.
# NOTE: install via ./install.sh, not `pip install -r requirements.txt` alone —
# mordred must be installed with --no-deps (it pins an old numpy), and torch
# comes from the PyTorch index. install.sh handles the ordering.
numpy==2.0.2
pandas==2.2.2
openpyxl
rdkit==2025.9.6
scipy==1.17.0
scikit-learn==1.8.0
imbalanced-learn==0.14.1
umap-learn==0.5.11
lightgbm==4.6.0
mendeleev
morfeus-ml==0.8.0
networkx==3.6.1
optuna==4.7.0
colorlog
drfp==0.3.7
dscribe
ase
sparse
joblib
transformers
shap
matplotlib
xgboost==3.2.0

# Installed separately by install.sh (see notes above):
#   mordred==1.2.0 (--no-deps)
#   torch==2.10.0 (PyTorch wheel index; CPU build used in Docker)

# Web app (app/) only:
#   streamlit, filelock (installed in Dockerfile)
